import React from 'react';
import styled from 'styled-components';

const Loading = () => (<Root>Loading tournaments ...</Root>);

const Root = styled.div`
  text-align: center;
`;

export default Loading;