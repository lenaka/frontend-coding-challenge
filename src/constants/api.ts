// It's not a good idea to store url in config.
// It leads to a problem with stages settings
// Has been moved to .env and can be rewritten in .env.local
// export const API_BASE_URL = 'http://localhost:4000';

export const API_TOURNAMENTS_URL = '/tournaments';
